import mockSchedule from "@mocks/mockItem/mockSchedule";
import { Schedule } from "@type/index";

const mockBestSchedule: Schedule[] = [mockSchedule[0], mockSchedule[1]];

export default mockBestSchedule;
